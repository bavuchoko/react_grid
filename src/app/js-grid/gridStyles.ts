export const GRID_BORDER = '#bdc2c9';
/** 헤더 정렬 아이콘 슬롯·`JsGridTable` sort 아이콘 크기와 동일 */
export const GRID_SORT_ICON_SLOT_PX = 14;
export const LINEAR_CELL_PADDING_X = 14;
export const LINEAR_CELL_INNER_GAP = 2;
/** `linear` 테마: 헤더 `th`의 패딩 + 왼쪽 sort 슬롯 + gap 과 본문 시작 위치를 맞춘다. */
export const LINEAR_DATA_CELL_PADDING_LEFT =
    LINEAR_CELL_PADDING_X + GRID_SORT_ICON_SLOT_PX + LINEAR_CELL_INNER_GAP;
export const CELL_MAX_WIDTH_PX = 280;
/** 사용자가 헤더 드래그로 줄일 수 있는 최소 열 너비 */
export const COL_RESIZE_MIN_PX = 130;
export const COL_RESIZE_MAX_PX = 640;
